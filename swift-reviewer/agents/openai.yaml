interface:
  display_name: "Swift Reviewer"
  short_description: "Read-only Swift change review and readiness"
  default_prompt: "Use $swift-reviewer to review these Swift changes and evidence without editing the implementation."
policy:
  allow_implicit_invocation: true
